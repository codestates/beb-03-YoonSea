import React from 'react';

const Create = () => {
  // user scenario
  // 현재 컴퓨터에서 이미지를 업로드 할 수 있어야한다 (팀원들과 결정)
  // or 이미지 URL을 업로드 할 수 있어야한다 (팀원들과 결정)
  // NFT 발행이 가능해야한다 <-

  // ipfs

  return <div></div>;
};

export default Create;
